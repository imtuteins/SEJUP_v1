spring.application.name=V2_Maven

# Database configuration (uses environment variables in production, defaults for local development)
spring.datasource.driver-class-name=${DATABASE_DRIVER:com.mysql.cj.jdbc.Driver}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/SEJUP3}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:kevin1}

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.generate-ddl=true

# JWT configuration
jwt.secret=${JWT_SECRET:w4f8K9vT1s2Uq3Xh7Jm5BvPzR6yN8LcQdE0fYpA1sVw=}
jwt.expiration=${JWT_EXPIRATION:3600}

# File upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server port (Render provides this automatically)
server.port=${PORT:9090}
